export enum LogicGateEnum {
  AND = 'AND',
  OR = 'OR',
  NOT = 'NOT',
  NAND = 'NAND',
  NOR = 'NOR',
  XOR = 'XOR',
  XNOR = 'XNOR',
}

export enum LogicGateName {
  A = 'A',
  B = 'B',
  Y = 'Y',
}

export enum LogicGateTypeName {
  INPUT = 'input',
  OUTPUT = 'output',
  DEFAULT = 'default',
  LITERAL = 'literal',
  LOGICGATE = 'logicGate',
}
